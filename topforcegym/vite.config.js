import { defineConfig } from 'vite';

export default defineConfig({
  base: '/topforce-admin/', // Ajusta esto si usas GitHub Pages
  server: {
    open: true // Abre el navegador automáticamente al iniciar
  }
});